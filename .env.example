# Environment Variables Configuration
# Copy this file to .env and modify according to your environment
# Different configurations for development and production are shown below

# =============================================================================
# ENVIRONMENT SELECTION
# =============================================================================
# Set to 'development' for local development or 'production' for deployment
NODE_ENV=development

# =============================================================================
# BACKEND CONFIGURATION
# =============================================================================
# JWT Secret - CRITICAL: Change this to a secure random string in production!
# Generate with: openssl rand -base64 32
JWT_SECRET=your-secure-jwt-secret-change-in-production

# Database configuration
DATABASE_PATH=./data/data.db

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================
# Frontend URL and host configuration
# Development example: http://localhost:5173
# Production example: https://your-domain.com
FRONTEND_URL=http://localhost:5173
FRONTEND_HOST=localhost
FRONTEND_PORT=5173

# =============================================================================
# SERVER PORTS
# =============================================================================
# HTTP and HTTPS ports for the reverse proxy
HTTP_PORT=80
HTTPS_PORT=443

# =============================================================================
# DATA STORAGE
# =============================================================================
# Data path for persistent storage
# Development: ./data
# Production: /var/lib/library-app/data (absolute path recommended)
DATA_PATH=./data

# =============================================================================
# API CONFIGURATION
# =============================================================================
# API URL for frontend to connect to backend
# Development: http://localhost:3000
# Production: https://api.your-domain.com
VITE_API_URL=http://localhost:3000

# =============================================================================
# EXAMPLE CONFIGURATIONS FOR DIFFERENT ENVIRONMENTS
# =============================================================================

# --- DEVELOPMENT CONFIGURATION ---
# NODE_ENV=development
# JWT_SECRET=dev-secret-key-change-in-production-please
# FRONTEND_URL=http://localhost:5173
# FRONTEND_HOST=localhost
# FRONTEND_PORT=5173
# DATA_PATH=./data
# VITE_API_URL=http://localhost:3000

# --- PRODUCTION CONFIGURATION ---
# NODE_ENV=production
# JWT_SECRET=your-super-secure-random-jwt-secret-here
# FRONTEND_URL=https://your-domain.com
# FRONTEND_HOST=your-domain.com
# FRONTEND_PORT=3001
# DATA_PATH=/var/lib/library-app/data
# VITE_API_URL=https://api.your-domain.com

# =============================================================================
# SECURITY NOTES FOR PRODUCTION
# =============================================================================
# 1. Change JWT_SECRET to a secure random string
# 2. Use HTTPS URLs for FRONTEND_URL and VITE_API_URL
# 3. Set DATA_PATH to an absolute path with proper permissions
# 4. Consider using Docker secrets or environment variable injection
# 5. Never commit .env files with production secrets to version control