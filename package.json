{
  "name": "library-management",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "domain",
    "apps/*"
  ],
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  },
  "scripts": {
    "build": "yarn workspaces foreach -vvpAt run build"
  }
}
